describe('the palindrome canary spec', () => {
  it('shows the infrastructure works', () => {
    true.should.be.true();
  });

  describe('palindrome should be', () => {
    it('true for mom');
    it('false for dude');
  });
});
